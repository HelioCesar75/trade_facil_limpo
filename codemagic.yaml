workflows:
  flutter-app:
    name: Build Android and iOS
    max_build_duration: 60
    environment:
      flutter: stable
      xcode: latest
    scripts:
      - name: Install Flutter dependencies
        script: flutter pub get
    artifacts:
      - build/**/*
    publishing:
      email:
        recipients:
          - cesar_emilly@hotmail.com
